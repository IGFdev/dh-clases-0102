<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
</head>

<body>
  <h1>Estás viendo los productos!</h1>
</body>

</html>

const productModel = {
    findAllProducts: () => {
        const productos = productosDB.traerTodos();

        return productos;
    },

    findById: id => {
        const productoBuscado = productosDB.traerPorId(id);

        return productoBuscado;
    }
}

const controller = {
    getIndex: (req, res) => {
        res.sendFile('./index.html');
    }
}